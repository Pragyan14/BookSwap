<!DOCTYPE html>
<html lang="en">

  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="/css/home.css" />
    <style>
      input {
        padding: 5px 12px;
        font-size: 14px;
        line-height: 20px;
        color: #24292e;
        vertical-align: middle;
        background-color: #ffffff;
        background-repeat: no-repeat;
        background-position: right 8px center;
        border: 1px solid black;
        width: 50%;
        height: 32px;
        font-size: 16px;
        border-radius: 4px;
        outline: none;
        box-shadow: rgba(225, 228, 232, 0.2) 0px 1px 0px 0px inset;
      }

      label {
        color: #24292e;
      }

      input:focus {
        border-color: #FF4742;
        outline: none;
      }

      .profilePic,
      .profileInput {
        background-color: #91C8E4;
        border: 2px solid red;
        border-radius: 8px;
      }

      .wrapper {
        display: grid;
        grid-template-columns: 1fr 2fr;
        grid-column-gap: 12px;
      }

      .inputRow {
        margin-bottom: 12px;
      }

      form {
        margin: 12px;
      }

      .alert {
  position: relative;
  top: 10;
  left: 0;
  width: auto;
  height: auto;
  padding: 10px;
  margin: 10px;
  line-height: 1.8;
  border-radius: 5px;
  cursor: hand;
  cursor: pointer;
  font-family: sans-serif;
  font-weight: 400;
}

.alertCheckbox {
  display: none;
}

:checked + .alert {
  display: none;
}

.alertText {
  display: table;
  margin: 0 auto;
  text-align: center;
  font-size: 16px;
}

.alertClose {
  float: right;
  padding-top: 5px;
  font-size: 10px;
}

.clear {
  clear: both;
}

.info {
  background-color: #EEE;
  border: 1px solid #DDD;
  color: #999;
}

.success {
  background-color: #A2FF86;
  /* border: 1px solid #DED; */
  color: black;
  display: none;
}



    </style>
  </head>

  <body>
    <div class="container">
      <h1>PROFILE</h1>
      <div class="wrapper">

        <div class="profilePic" style="text-align: center;">
          <img style="width: 70%; " src="/images/profile.png" alt="profilePic">
          <h2>Your Profile</h2>
        </div>
        <div class="profileInput">

          <label>
            <input type="checkbox" class="alertCheckbox" autocomplete="off" />
            <div class="alert success">
              <span class="alertClose">X</span>
              <span class="alertText">SUCCESS</span>
            </div>
          </label>

          <form action="/profile" method="post" id="profileForm">
            <div class="inputRow">
              <label for="email">Eamil</label><br>
              <input type="text" id="email" name="email" value="<%= user.email %>" disabled required />
            </div>
            <div class="inputRow">
              <label for="full_name">Name</label> <br>
              <input type="text" id="full_name" name="full_name" placeholder="Full name" value="<%= user.full_name %>" required />
            </div>
            <div class="inputRow">
              <label for="contact">Contact</label><br>
              <input type="text" id="contact" name="contact" placeholder="Contact" value="<%= user.contact %>" required />
            </div>
            <div class="inputRow">
              <label for="adrress">Adrress</label><br>
              <input type="text" id="adrress" name="address" placeholder="Vijay Nagar, Indore" value="<%= user.address %>" required />
            </div>
            <button type="submit" class="button-24">Save & Submit</button>
          </form>
        </div>
      </div>
    </div>
  </body>
  <script src="https://code.jquery.com/jquery-3.7.0.min.js"></script>
  <script src="/js/index.js"></script>

</html>